// Name: Sofia Nunes 
// Date: 11-07-2022
// Motive: input styling component with Material UI

import { createTheme } from '@mui/material/styles';

// Color Variables
export const colorTheme = createTheme({
  palette: {
    primary: {
      main: '#D99F84',
    },
  },
});